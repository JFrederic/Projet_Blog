{% trans_default_domain 'FOSUserBundle' %}


<div class="container">


{{ form_start(form, { 'action': path('fos_user_change_password'), 'attr': { 'class': 'fos_user_change_password' } }) }}
   {{ form_widget(form) }}
   <div class="center-align">
       <button class="waves-effect waves-light btn deep-purple lighten-1" type="submit" value="{{ 'change_password.submit'|trans }}">Modifier</button>
   </div>
{{ form_end(form) }}
</div>
